# Framework Configuration
framework.name=Selenium BrowserStack Automation Framework
framework.version=1.0.0

# Environment Configuration
# Options: local, browserstack
execution.environment=browserstack

# Test Execution Settings
default.timeout=60
implicit.wait=15
explicit.wait=60
page.load.timeout=120

# Screenshot Settings
screenshot.on.failure=true
screenshot.on.pass=false

# BrowserStack Settings (Multiple fallback options)
# Option 1: Environment variables (recommended for CI/CD)
browserstack.username=${BROWSERSTACK_USERNAME}
browserstack.accesskey=${BROWSERSTACK_ACCESS_KEY}

# Option 2: Direct values (for local testing - uncommented for debugging)
browserstack.username=gauravkhurana_yShwDZ
browserstack.accesskey=VGzb8uFGpPF7XWjXzcsG

browserstack.hub.url=https://hub-cloud.browserstack.com/wd/hub

# Local Testing Settings
local.browser=chrome
local.headless=false

# Reporting Settings
extent.report.path=test-output/extent-reports/
extent.report.name=Test Execution Report

# Application URLs
app.url.local=https://testathon.live/
app.url.staging=https://testathon.live/
app.url.production=https://testathon.live/
app.url.browserstack=https://testathon.live/

# Test Data
test.data.path=src/test/resources/testdata/
